/**
 * Content IDs for the "Las crónicas" / "The chronicles" section, in
 * display order. Per the i18n design (D7), the translatable prose for
 * each entry (when/title/description) lives in `src/i18n/{es,en}.json`
 * under `experience.entries.<id>`, keyed by this `id` — this module only
 * holds the structural ordering, not the language-dependent text.
 */
export const experience = [{ id: 'cafam-q10' }, { id: 'sena' }];
